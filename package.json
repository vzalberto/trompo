{
  "name": "zzzoylacerda",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0",
    "react-spring": "^8.0.27",
    "react-zdog": "^1.0.10",
    "tone": "^13.8.25",
    "uifx": "^2.0.3",
    "zdog": "^1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "fuego": "npm run build && rm -rf deploy/public/* && cp -rf build/* deploy/public/ && cd deploy && firebase deploy && cd .."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
